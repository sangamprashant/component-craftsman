import { Table } from "../../components";

export default {
  component: Table,
  parameters: {
    layout: "centered",
  },
  tags: ["autodocs"],
};

export const Placeholder = {
  args: {
    placeholder: "Enter the email",
  },
};
